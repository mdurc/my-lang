/*
mut x: i64 = 10;
mut y: i64 = (x + 5) * 2;
mut z : string = "hi";

print x;
asm { call print_newline };
print y;
asm { call print_newline };
*/

func main() {
  mut a : i32;
  a = 2 + a;
  a = 3;
  print a;
  print "\n";

  b : string = "hi";
  print b;
  print "\n";

  // spillover occurs which is unimpl, if we leave this
  // c : bool = true;
  // print c;
  // print "\n";
}
